// import React, {useContext, useEffect} from "react";
// import noteContext from "../context/notes/noteContext";   
const About = () => {
  // const d = useContext(noteContext)
  // useEffect(()=>{
  //   d.update();
  // }, [d]);
  //  is is used to show the details of usecontext {d.state.name} and he is persuing {d.state.class}.
  return (
    <div>
      This is About 
    </div>
  )
}

export default About
